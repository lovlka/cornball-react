import React, { Component, PropTypes } from 'react';

export default class Game extends Component {

   render() {
      return (
         <div id="game"></div>
      );
   }

}

Game.propTypes = {
};